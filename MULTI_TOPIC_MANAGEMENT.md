# 多议题并行管理功能

## 需求分析

需求文档指出：
> "议题管理混乱：用户希望同时探索多个议题，但不同讨论的上下文、进展与结论难以并行隔离管理"

## 当前实现状态

### ✅ 已实现功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 多讨论并行 | ✅ | 支持同时存在多个讨论 |
| 讨论隔离 | ✅ | 每个讨论独立的上下文、发言、共识分歧 |
| 独立状态 | ✅ | 每个讨论独立的状态管理（assembling/active/concluded） |
| 讨论列表 | ✅ | 首页展示所有讨论 |
| 快速进入 | ✅ | 点击讨论卡片进入演播厅 |
| 独立演播厅 | ✅ | 每个讨论有独立的演播厅页面 |

### ❌ 待实现功能

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 讨论标签/分类 | 高 | 按话题类型分类讨论 |
| 讨论搜索 | 高 | 搜索特定讨论 |
| 讨论筛选 | 高 | 按状态、时间筛选 |
| 讨论对比 | 中 | 对比多个讨论的结果 |
| 讨论模板 | 中 | 常用话题模板 |
| 同时运行 | 中 | 同时运行多个讨论 |
| 讨论导出 | 低 | 导出讨论结果 |
| 讨论分享 | 低 | 分享讨论链接 |

## 技术实现

### 1. 数据库隔离

```sql
-- 每个讨论独立存储
discussions (id, topic, status, ...)
participants (discussion_id, ...)  -- 外键关联
speeches (discussion_id, ...)      -- 外键关联
findings (discussion_id, ...)      -- 外键关联
```

### 2. API 隔离

```python
# 每个 API 都需要 discussion_id
GET /api/discussions/{id}
GET /api/discussions/{id}/status
GET /api/discussions/{id}/events (SSE)
```

### 3. 前端隔离

```tsx
// 每个讨论独立的路由
<Route path="/studio/:discussionId" element={<StudioPage />} />
```

## 用户体验流程

### 场景：同时探索多个议题

1. **创建多个讨论**
   - 用户可以在首页创建多个讨论
   - 每个讨论有独立的话题和嘉宾

2. **并行管理**
   - 首页展示所有讨论的状态
   - 用户可以随时进入任意讨论

3. **独立演播厅**
   - 每个讨论有独立的演播厅
   - 讨论之间互不干扰

4. **结果对比**
   - 用户可以查看多个讨论的结果
   - 对比不同话题的共识和分歧

## 示例：多议题并行

```
┌─────────────────────────────────────────────────────────────┐
│  AI Panel Studio - 讨论列表                                  │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 🟢 AI 会取代程序员吗？                                   ││
│  │    参与者: 4人 | 轮次: 2/2 | 共识度: 65%                 ││
│  │    [进入演播厅]                                          ││
│  └─────────────────────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 🟡 远程办公 vs 现场办公                                  ││
│  │    参与者: 3人 | 轮次: 1/3 | 进行中                      ││
│  │    [进入演播厅]                                          ││
│  └─────────────────────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 🔴 数字货币的前景                                        ││
│  │    参与者: 5人 | 轮次: 3/3 | 已结束                      ││
│  │    [查看结果]                                            ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

## 后续优化计划

### 第一阶段（高优先级）

1. **讨论标签系统**
   - 添加标签字段
   - 按标签筛选讨论
   - 标签云展示

2. **讨论搜索**
   - 按话题搜索
   - 按参与者搜索
   - 按内容搜索

3. **讨论筛选**
   - 按状态筛选（进行中/已结束）
   - 按时间筛选
   - 按共识度筛选

### 第二阶段（中优先级）

4. **讨论对比**
   - 选择多个讨论
   - 对比共识和分歧
   - 生成对比报告

5. **讨论模板**
   - 预设热门话题
   - 一键创建讨论
   - 自定义模板

6. **同时运行**
   - 后台运行讨论
   - 进度通知
   - 批量操作

### 第三阶段（低优先级）

7. **讨论导出**
   - 导出 Markdown
   - 导出 PDF
   - 导出 JSON

8. **讨论分享**
   - 生成分享链接
   - 嵌入代码
   - 社交媒体分享

## 测试验证

### 测试1：创建多个讨论

```python
# 创建讨论1
resp1 = requests.post('/api/discussions', json={
    'topic': 'AI 会取代程序员吗？',
    'host': {...},
    'experts': [...]
})

# 创建讨论2
resp2 = requests.post('/api/discussions', json={
    'topic': '远程办公的优缺点',
    'host': {...},
    'experts': [...]
})

# 验证两个讨论独立存在
assert resp1.json()['discussion_id'] != resp2.json()['discussion_id']
```

### 测试2：讨论隔离

```python
# 获取两个讨论的状态
status1 = requests.get(f'/api/discussions/{disc_id1}/status')
status2 = requests.get(f'/api/discussions/{disc_id2}/status')

# 验证状态独立
assert status1.json()['current_round'] != status2.json()['current_round']
```

## 总结

当前已实现**基本的多议题并行管理**：
- ✅ 多讨论并行存在
- ✅ 讨论上下文隔离
- ✅ 独立状态管理
- ✅ 独立演播厅

后续需要完善：
- ❌ 讨论标签/分类
- ❌ 讨论搜索
- ❌ 讨论对比
- ❌ 讨论模板
