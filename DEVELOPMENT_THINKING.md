# 开发过程思路 & 工作流说明

## 一、开发流程概述

本次开发采用 **Claude Code + Deepseek V4 Pro** 作为核心开发工具，遵循 **SDD → DDD → TDD → E2E** 的工程化开发范式，从零构建了一个 AI 圆桌讨论 Web 应用 MVP。

### 1.1 开发环境

- **IDE**: Claude Code（Superpowers 插件）
- **AI 模型**: Deepseek V4 Pro
- **前端**: Next.js 14 + TypeScript + Tailwind CSS
- **后端**: Python FastAPI + SQLite
- **版本控制**: Git

### 1.2 阶段化开发流程

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   SDD 阶段  │ -> │   DDD 阶段  │ -> │  功能实现   │ -> │  测试完善   │
│  数据建模   │    │  UI/UX 设计 │    │  后端核心   │    │  TDD + E2E  │
│  API 契约   │    │  组件开发   │    │  前端交互   │    │  文档完善   │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
     (Day 1)           (Day 2)           (Day 3)           (Day 4)
```

**Day 1 - SDD（契约/模型驱动）**
- 分析业务需求，识别核心实体
- 设计数据库 Schema（5 张表）
- 定义 Pydantic Schema（API 契约）
- 编写 dataclass 数据模型

**Day 2 - DDD（设计驱动）**
- 设计演播厅视觉规范（深色主题、CSS 变量）
- 实现根布局和导航栏
- 实现首页（讨论列表 + 快速创建）
- 实现演播厅页面（控制栏 + 参与者面板 + 讨论记录）

**Day 3 - 功能实现**
- 实现 SSE 事件管理器
- 实现数据库操作层
- 实现 LLM API 调用封装
- 实现业务逻辑核心
- 实现 API 路由

**Day 4 - 测试完善**
- 编写单元测试（数据库/业务/API）
- 编写 E2E 测试脚本
- 性能优化（并行化）
- 完善文档

---

## 二、与 AI 协同中遇到的典型问题及解决路径

### 问题 1：AI 幻觉导致的 API 不一致

**现象**：
在让 AI 生成后端代码时，不同批次生成的代码存在 API 不一致的问题。例如，第一批生成的 `PanelDB` 类使用 `create_discussion()` 方法名，但第二批生成的路由代码却调用 `add_discussion()`。

**原因分析**：
- AI 缺乏全局上下文，每次对话只能看到局部信息
- 不同 Prompt 的表述差异导致 AI 生成不同的方法名

**解决路径**：
1. **前置 Schema 定义**：在 SDD 阶段精确定义 API 契约，后续 Prompt 都引用同一份契约
2. **代码审查机制**：每次 AI 生成代码后，人工检查方法名、参数名的一致性
3. **增量式生成**：避免一次性生成过多代码，分模块逐步构建

**经验总结**：
> "先定义契约，再实现细节" 是控制 AI 幻觉的有效手段。SDD 阶段的 Schema 定义为后续开发提供了明确的接口规范。

---

### 问题 2：前后端联调时的数据格式不匹配

**现象**：
前端 TypeScript 类型定义与后端 Pydantic Schema 存在字段名不一致的问题。例如，后端返回 `finding_type`，但前端定义的是 `type`。

**原因分析**：
- 前后端分别由 AI 生成，缺乏统一的类型定义源
- Python 的 snake_case 与 TypeScript 的 camelCase 命名习惯不同

**解决路径**：
1. **统一类型定义**：在 `docs/API.md` 中定义完整的字段规范
2. **前端适配层**：在 `lib/api.ts` 中添加字段映射逻辑
3. **类型检查**：利用 TypeScript 的类型检查能力，在编译期发现不匹配

**经验总结**：
> API 文档不仅是给开发者看的，更是给 AI 看的。在 Prompt 中引用统一的 API 文档，可以显著减少格式不匹配的问题。

---

### 问题 3：SSE 长连接的稳定性问题

**现象**：
在自动运行讨论时，SSE 连接会偶尔断开，导致前端无法接收后续事件。

**原因分析**：
- 浏览器对 SSE 连接有超时限制
- 后端异步任务执行时间过长时，连接可能被中间代理断开

**解决路径**：
1. **心跳机制**：每 30 秒发送一次 `ping` 事件，保持连接活跃
2. **自动重连**：前端 `EventSource` 原生支持自动重连，但需要处理状态恢复
3. **事件持久化**：将事件存储在数据库中，重连后可以补发丢失的事件

**经验总结**：
> 实时通信是 Web 应用的难点之一。SSE 相比 WebSocket 更简单，但需要特别注意连接管理和错误恢复。

---

## 三、对"工程化 AI 开发"的理解

### 3.1 核心理念：人机协作，而非人机替代

工程化 AI 开发不是让 AI 替代开发者，而是让 AI 成为开发者的"超级助手"。开发者的核心价值在于：

1. **需求理解**：理解业务需求，识别核心问题
2. **架构设计**：设计系统架构，做出技术决策
3. **质量把控**：审查 AI 输出，确保代码质量
4. **问题调试**：定位和修复 AI 无法解决的问题

AI 的核心价值在于：

1. **代码生成**：快速生成样板代码，提升开发效率
2. **模式识别**：识别常见模式，提供最佳实践
3. **知识检索**：快速检索技术文档和示例代码
4. **重复劳动**：处理重复性的编码工作

### 3.2 关键实践：多范式融合

本次开发采用了 SDD + DDD + TDD 的多范式融合方法：

- **SDD（契约驱动）**：先定义数据模型和 API 契约，再实现业务逻辑
  - 优点：接口清晰，前后端可并行开发
  - 适用场景：数据密集型应用

- **DDD（设计驱动）**：先设计 UI/UX，再实现前端组件
  - 优点：用户体验优先，视觉一致性好
  - 适用场景：用户界面开发

- **TDD（测试驱动）**：先写测试，再写实现
  - 优点：代码质量高，重构有保障
  - 适用场景：核心业务逻辑

**多范式融合的价值**：
不同阶段采用不同的开发范式，可以最大化每个阶段的效率。SDD 阶段确保数据模型的正确性，DDD 阶段确保用户体验的一致性，TDD 阶段确保代码质量的可靠性。

### 3.3 Prompt 工程的重要性

与 AI 协同开发的核心技能是 **Prompt 工程**——如何用结构化的语言引导 AI 生成高质量代码。

**有效 Prompt 的特征**：
1. **明确的目标**：清楚地说明要实现什么功能
2. **具体的约束**：指定技术栈、命名规范、错误处理要求
3. **上下文引用**：引用已有的 Schema、类型定义、代码示例
4. **分步骤拆解**：将复杂任务拆分为多个简单任务

**示例对比**：
```
❌ 无效 Prompt：
"请实现一个讨论功能"

✅ 有效 Prompt：
"请实现讨论创建接口 (POST /api/discussions)：
请求体：CreateDiscussionRequest (topic: str, expert_count: int, max_rounds: int)
响应：DiscussionResponse
使用 PanelCore.create_discussion() 方法
返回 201 状态码，Location header 指向新创建的讨论"
```

### 3.4 工程化 AI 开发的未来

随着 AI 能力的提升，工程化 AI 开发将呈现以下趋势：

1. **AI 参与架构设计**：AI 不仅能生成代码，还能参与架构决策
2. **自动化测试生成**：根据代码自动生成测试用例
3. **智能代码审查**：AI 自动发现代码中的问题和改进点
4. **持续学习**：AI 从项目历史中学习，提升代码生成质量

但无论 AI 如何发展，开发者的核心价值——**问题定义、架构设计、质量把控、创新思考**——始终不可替代。

---

## 四、总结

本次开发实践验证了 **Claude Code + Deepseek V4 Pro** 在工程化开发中的可行性。通过 SDD → DDD → TDD → E2E 的阶段化方法，我们成功构建了一个具有生产级质感的 AI 圆桌讨论 Web 应用 MVP。

**关键收获**：
1. 工程化拆解是控制 AI 幻觉的有效手段
2. 多范式融合可以最大化每个阶段的效率
3. Prompt 工程是人机协作的核心技能
4. 测试是保障代码质量的最后防线

**后续改进方向**：
1. 专家自主发言机制（举手/抢答/补充/反驳）
2. 更丰富的 UI 交互动画
3. 讨论模板系统
4. 多语言支持
